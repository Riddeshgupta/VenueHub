# Frontend (VenueHub)

This directory will contain the React + JavaScript frontend application for VenueHub.

## Planned Directory Structure
- `public/`: Static assets and index HTML template
- `src/`: React source code (components, pages, services, styles)
