# Backend (VenueHub)

This directory will contain the Python + Flask REST API backend application for VenueHub.

## Planned Directory Structure
- `app/`: Application modules (routes, models, controllers, services)
- `config/`: Application configuration settings and environment setups
- `requirements.txt`: Python package dependencies specification
